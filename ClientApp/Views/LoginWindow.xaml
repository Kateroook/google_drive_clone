<Window x:Class="ClientApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientApp.Views"
        mc:Ignorable="d"
        Title="Login" Height="500" Width="400" WindowStartupLocation="CenterScreen" Background="#F5F5F5">
    <Grid>
        <Border Background="White" 
                CornerRadius="10" 
                Margin="30"
                Padding="40"
                Effect="{DynamicResource DropShadow}">

            <StackPanel VerticalAlignment="Center">
                <!-- Logo -->
                <TextBlock Text="☁" 
                           FontSize="72" 
                           HorizontalAlignment="Center"
                           Foreground="#1976D2"
                           Margin="0,0,0,10"/>

                <TextBlock Text="My Cloud Drive" 
                           FontSize="24" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="#333333"
                           Margin="0,0,0,30"/>

                <!-- Status Text -->
                <TextBlock x:Name="StatusText" 
                           Text="Sign in to access your files" 
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Foreground="#666666"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,0,0,30"/>

                <!-- Login Button -->
                <Button x:Name="LoginButton" 
                        Content="Sign in with Google" 
                        Click="LoginButton_Click"
                        Height="50"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Background="#4285F4"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0" 
                                                        Background="White" 
                                                        Width="40" 
                                                        Height="40"
                                                        CornerRadius="3"
                                                        Margin="5">
                                                    <Image Source="https://www.google.com/images/branding/googleg/1x/googleg_standard_color_128dp.png" 
                                                           Width="25" 
                                                           Height="25"/>
                                                </Border>

                                                <ContentPresenter Grid.Column="1" 
                                                                HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#3367D6"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#BDBDBD"/>
                                    <Setter Property="Cursor" Value="Wait"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Loading Indicator -->
                <StackPanel x:Name="LoadingPanel" 
                           Visibility="Collapsed" 
                           Orientation="Horizontal"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0">
                    <TextBlock Text="⌛" FontSize="20" Margin="0,0,10,0"/>
                    <TextBlock Text="Authenticating..." FontSize="14" Foreground="#666666"/>
                </StackPanel>

                <!-- Info Text -->
                <TextBlock Text="Secure sign-in with your Google account" 
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Foreground="#999999"
                           Margin="0,30,0,0"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
