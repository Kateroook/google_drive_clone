# google_drive_clone
A multi-client cloud storage application inspired by Google Drive. The project includes a React-based web client, a C# WPF desktop client with unit test coverage, and a Node.js/Express backend. Client synchronization is achieved via polling, enabling consistent file updates across platforms.
